pipeline {			
    agent any
    stages {			
        stage('Build') {			
            steps {			
                echo 'DEVELOP MASTER UPSTREAM executed'		
                     }			
        }			
   }		
}
